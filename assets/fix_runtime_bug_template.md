# 运行类 Bug 修复方案：<Bug 名称>

## 0. 文档信息

Bug 名称：

Bug 类型：运行类 Bug

方案状态：draft

创建时间：

最后更新时间：

方案文件路径：

关联代码实现任务：

------

## 1. Bug 概要

### 1.1 Bug 现象

说明程序在哪里无法正常运行：

- ...

### 1.2 触发场景

说明什么操作、命令、接口或任务会触发该问题：

- ...

### 1.3 影响范围

说明该 Bug 影响哪些能力：

- 程序启动：
- 构建：
- 测试：
- 接口：
- 页面：
- 任务队列：
- 定时任务：
- 数据库：
- 其他：

------

## 2. 复现方式

### 2.1 复现步骤

1. ...
2. ...
3. ...

### 2.2 复现命令

```
...
```

### 2.3 复现输入

```
...
```

### 2.4 复现环境

- 操作系统：
- 运行时版本：
- 包管理工具：
- 依赖安装方式：
- 数据库：
- 缓存：
- 环境变量：
- 其他：

------

## 3. 报错和日志

### 3.1 完整报错

```
...
```

### 3.2 堆栈信息

```
...
```

### 3.3 关键错误行

```
...
```

### 3.4 日志文件位置

```
...
```

### 3.5 报错关键词

- ...

------

## 4. 运行环境和配置检查

### 4.1 启动 / 执行入口

- 文件：
- 函数：
- 命令：
- 路由：
- 任务：

### 4.2 依赖检查

- 依赖声明文件：
- 相关依赖：
- 当前版本：
- 是否缺失：
- 是否版本冲突：

### 4.3 配置检查

- 配置文件：
- 环境变量：
- 路由配置：
- 权限配置：
- 中间件配置：
- 任务队列配置：
- 缓存配置：
- 第三方服务配置：

### 4.4 数据库 / 外部服务检查

- 数据库连接：
- 数据表是否存在：
- migration 是否执行：
- 外部服务是否可访问：
- 凭证是否存在：

------

## 5. 已确认事实

只记录已经从报错、日志、代码或配置中确认的事实。

1. ...
2. ...
3. ...

------

## 6. 待确认假设

记录尚未完全确认，但可能相关的假设。

| 假设 | 依据 | 需要如何验证 |
| ---- | ---- | ------------ |
| ...  | ...  | ...          |

------

## 7. 可能原因列表

| 可能原因 | 支持证据 | 反证 / 不确定点 | 可能性       |
| -------- | -------- | --------------- | ------------ |
| ...      | ...      | ...             | 高 / 中 / 低 |

------

## 8. 最可能原因

### 8.1 判断结果

最可能原因是：

```
...
```

### 8.2 判断依据

1. ...
2. ...
3. ...

### 8.3 排除的原因

1. 原因：
   - 排除依据：
2. 原因：
   - 排除依据：

------

## 9. 修复方案

### 9.1 修复目标

修复后应达到：

1. 程序可以正常运行到目标路径
2. 原报错不再出现
3. 不引入新的运行错误
4. 不破坏无关功能

### 9.2 修复策略

说明准备如何修复，不写具体代码。

1. ...
2. ...
3. ...

### 9.3 最小修改原则

- 是否只修改导致运行失败的必要位置：
- 是否避免重构无关代码：
- 是否避免改变业务行为：
- 是否避免引入新依赖：
- 是否保持旧接口不变：
- 是否保持旧数据结构不变：

------

## 10. 文件变更计划

### 10.1 新增文件

1. `path/to/new_file`
   - 作用：
   - 新增原因：

如果不需要新增文件，写：

```
本次修复不需要新增文件。
```

### 10.2 修改文件

1. `path/to/existing_file`
   - 修改位置：
   - 当前问题：
   - 修改原因：
   - 修改内容概述：
   - 是否影响旧逻辑：

### 10.3 删除文件

1. `path/to/deleted_file`
   - 删除原因：
   - 风险：

如果不需要删除文件，写：

```
本次修复不删除已有文件。
```

------

## 11. 依赖、扩展包和配置变化

### 11.1 依赖变化

是否新增第三方依赖：

是否新增框架扩展包：

是否升级已有依赖：

是否修改依赖版本：

是否新增系统组件或运行环境支持：

### 11.2 配置变化

是否修改配置文件：

是否新增环境变量：

是否修改路由配置：

是否修改权限配置：

是否修改中间件配置：

是否修改任务队列配置：

是否修改缓存配置：

是否修改第三方服务配置：

是否影响部署：

### 11.3 具体说明

如果有变化，逐项说明：

1. 变更项：
   - 文件位置：
   - 修改位置：
   - 修改原因：
   - 影响范围：
   - 是否有替代方案：

如果没有变化，写：

```
本次修复不需要新增依赖、扩展包或配置变化。
```

------

## 12. 数据影响

### 12.1 是否影响数据库结构

是 / 否

说明：

```
...
```

### 12.2 是否影响已有数据

是 / 否

说明：

```
...
```

### 12.3 是否需要数据迁移

需要 / 不需要

说明：

```
...
```

如果不涉及数据，写：

```
本次修复不涉及数据库结构、已有数据或数据迁移。
```

------

## 13. 验证方式

### 13.1 原报错验证

1. 执行：

   ```
   ...
   ```

2. 期望：

   ```
   ...
   ```

### 13.2 启动 / 构建 / 测试验证

1. ...
2. ...
3. ...

### 13.3 相关功能回归验证

1. ...
   - 操作：
   - 期望结果：

### 13.4 不应受影响的功能

1. ...
2. ...

------

## 14. 风险和回滚思路

### 14.1 主要风险

| 风险 | 影响范围 | 严重程度     | 预防方式 |
| ---- | -------- | ------------ | -------- |
| ...  | ...      | 高 / 中 / 低 | ...      |

### 14.2 回滚思路

1. ...
2. ...

### 14.3 不可逆变化说明

如果存在不可逆变化，说明：

1. 不可逆项：
   - 原因：
   - 影响：
   - 是否可以接受：

如果不存在不可逆变化，写：

```
本次修复不涉及不可逆变化。
```

------

## 15. 用户审查记录

### 第 1 次审查

用户反馈：

修改内容：

修改时间：

### 第 2 次审查

用户反馈：

修改内容：

修改时间：

### 第 3 次审查

用户反馈：

修改内容：

修改时间：