# 添加功能方案：<功能名称>

## 0. 文档信息

功能名称：

方案类型：老项目 / 添加功能

方案状态：draft

创建时间：

最后更新时间：

方案文件路径：

关联需求：

关联代码实现任务：

------

## 1. 功能边界

### 1.1 本次实现

- ...

### 1.2 本次不实现

- ...

------

## 2. 文件变更计划

### 2.1 新增文件

1. `path/to/new_file`
   - 作用：
   - 新增原因：

如果不需要新增文件，写：

```
本功能不需要新增文件。
```

### 2.2 修改文件

1. `path/to/existing_file`
   - 修改位置：
   - 修改原因：
   - 是否影响旧逻辑：

如果不需要修改旧文件，写：

```
本功能不需要修改已有文件。
```

------

## 3. 依赖、扩展包和配置变化

### 3.1 依赖变化

是否新增第三方依赖：

是否新增框架扩展包：

是否升级已有依赖：

是否修改依赖版本：

是否新增系统组件或运行环境支持：

### 3.2 配置变化

是否修改配置文件：

是否新增环境变量：

是否修改路由配置：

是否修改权限配置：

是否修改中间件配置：

是否修改任务队列配置：

是否修改缓存配置：

是否修改第三方服务配置：

是否影响部署：

### 3.3 具体说明

如果有变化，逐项说明：

1. 变更项：
   - 文件位置：
   - 修改位置：
   - 修改原因：
   - 影响范围：
   - 是否有替代方案：

如果没有变化，写：

```
本功能不需要新增依赖、扩展包或配置变化。
```

------

## 4. 最小修改原则

说明本方案如何做到最小改动：

- 是否只修改实现该功能必须修改的地方：
- 是否避免重构无关代码：
- 是否保持旧接口格式不变：
- 是否保持旧数据结构不变：
- 是否避免调整目录结构：
- 是否避免引入新依赖：
- 是否保留旧逻辑：

如果必须修改旧逻辑，说明原因和影响范围：

```
...
```

------

## 5. 函数设计和调用链

### 5.1 调用链

```
用户操作 / 外部输入
↓
入口函数 A
↓
业务函数 B
↓
数据处理函数 C
↓
数据库 / 外部服务 / 文件系统
↓
返回结果
```

### 5.2 函数说明

#### 函数 1：xxx(...)

所在文件：

职责：

输入：

输出：

调用关系：

- 被谁调用：
- 调用谁：

#### 函数 2：yyy(...)

所在文件：

职责：

输入：

输出：

调用关系：

- 被谁调用：
- 调用谁：

------

## 6. 数据结构说明

### 6.1 接口请求结构

```
...
```

### 6.2 接口响应结构

```
...
```

### 6.3 service 层输入结构

```
...
```

### 6.4 service 层输出结构

```
...
```

### 6.5 数据库新增字段

```
...
```

### 6.6 枚举值

```
...
```

### 6.7 队列消息结构

```
...
```

### 6.8 缓存结构

```
...
```

### 6.9 配置结构

```
...
```

### 6.10 前后端交互结构

```
...
```

字段说明要求：

- 字段名：
- 底层类型：
- 含义：
- 示例值：
- 是否必填：
- 可能取值：

数组说明要求：

```
字段名：xxx
类型：list[dict]

数组元素结构：

{
  "field_a": "str",
  "field_b": "int"
}
```

对象说明要求：

```
结构名：XxxObject

{
  "field_a": "str",
  "field_b": "int"
}
```

------

## 7. 简单用例和完整数据流

### 7.1 示例场景

用户要：

### 7.2 原始输入

```
...
```

### 7.3 第 1 层：入口函数 xxx()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

### 7.4 第 2 层：业务函数 yyy()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

### 7.5 第 3 层：数据处理 / 数据访问函数 zzz()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

### 7.6 最终输出

```
...
```

------

## 8. 异常和边界情况

| 场景           | 处理方式 | 返回结果 |
| -------------- | -------- | -------- |
| 输入为空       | ...      | ...      |
| 参数类型错误   | ...      | ...      |
| 数据不存在     | ...      | ...      |
| 数据重复       | ...      | ...      |
| 状态不允许     | ...      | ...      |
| 权限不足       | ...      | ...      |
| 并发重复提交   | ...      | ...      |
| 外部接口失败   | ...      | ...      |
| 数据库写入失败 | ...      | ...      |
| 旧数据不兼容   | ...      | ...      |

------

## 9. 对旧功能的影响

### 9.1 旧接口影响

影响：无 / 有

原因：

### 9.2 旧数据影响

影响：无 / 有

原因：

### 9.3 旧状态影响

影响：无 / 有

原因：

### 9.4 旧页面影响

影响：无 / 有

原因：

### 9.5 旧任务流程影响

影响：无 / 有

原因：

### 9.6 旧测试影响

影响：无 / 有

原因：

### 9.7 是否需要数据迁移

需要 / 不需要

原因：

------

## 10. 测试和验收方案

### 10.1 正常流程测试

1. ...
   - 输入：
   - 期望输出：

### 10.2 异常流程测试

1. ...
   - 输入：
   - 期望输出：

### 10.3 边界情况测试

1. ...
   - 输入：
   - 期望输出：

### 10.4 旧功能回归测试

1. ...
   - 操作：
   - 期望结果：

### 10.5 手动验证步骤

1. ...
2. ...
3. ...

### 10.6 验收标准

做到以下结果，才算方案完整：

1. ...
2. ...
3. ...

------

## 11. 风险和回滚思路

### 11.1 主要风险

1. ...
2. ...

### 11.2 回滚思路

1. ...
2. ...

------

## 12. 用户审查记录

### 第 1 次审查

用户反馈：

修改内容：

修改时间：

### 第 2 次审查

用户反馈：

修改内容：

修改时间：