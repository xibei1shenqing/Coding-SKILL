# 实现报告：<任务名称>

## 0. 文档信息

任务名称：

实现类型：

- 新项目
- 添加功能
- 修改功能
- 修复 Bug / 运行类
- 修复 Bug / 行为类
- 重构优化

实现状态：

- done
- partial
- blocked

创建时间：

最后更新时间：

方案文件路径：

```
docs/ai-plans/<plan_type>/<task_name>.md
```

实现报告路径：

```
docs/ai-implementation/<plan_type>/<task_name>.md
```

关联代码分支：

关联提交：

------

## 1. 使用的方案文件

本次代码实现基于以下方案文件：

```
docs/ai-plans/<plan_type>/<task_name>.md
```

方案文件中的核心目标：

- ...

本次实现范围：

- ...

本次不实现范围：

- ...

------

## 2. 实现概要

### 2.1 本次完成内容

- ...
- ...
- ...

### 2.2 本次未完成内容

如果全部完成，写：

```
本次已完成方案文件中要求的实现内容。
```

如果未全部完成，逐项说明：

| 未完成内容 | 对应方案章节 | 原因 | 后续处理建议 |
| ---------- | ------------ | ---- | ------------ |
| ...        | ...          | ...  | ...          |

### 2.3 本次实现结果说明

简要说明代码实现后的结果：

```
...
```

------

## 3. 方案内容完成情况

逐项列出方案中的关键内容是否已经实现。

| 方案内容 | 对应方案章节 | 完成状态                   | 实现位置       | 说明 |
| -------- | ------------ | -------------------------- | -------------- | ---- |
| ...      | 第 X 节：... | 已完成 / 部分完成 / 未完成 | `path/to/file` | ...  |

示例：

| 方案内容       | 对应方案章节              | 完成状态 | 实现位置                         | 说明         |
| -------------- | ------------------------- | -------- | -------------------------------- | ------------ |
| 新增导出接口   | 第 4 节：函数设计和调用链 | 已完成   | `app/routes/export.py`           | 新增接口入口 |
| Excel 生成逻辑 | 第 5 节：数据结构说明     | 已完成   | `app/services/export_service.py` | 生成导出文件 |
| 路由注册       | 第 2 节：文件变更计划     | 已完成   | `app/main.py`                    | 注册导出路由 |

------

## 4. 方案章节到代码位置映射

### 4.1 第 1 节：<章节名称>

方案要求：

```
...
```

实现位置：

| 文件           | 函数 / 类 / 接口 / 命令 | 实现内容 |
| -------------- | ----------------------- | -------- |
| `path/to/file` | `xxx()`                 | ...      |

### 4.2 第 2 节：<章节名称>

方案要求：

```
...
```

实现位置：

| 文件           | 函数 / 类 / 接口 / 命令 | 实现内容 |
| -------------- | ----------------------- | -------- |
| `path/to/file` | `xxx()`                 | ...      |

### 4.3 第 3 节：<章节名称>

方案要求：

```
...
```

实现位置：

| 文件           | 函数 / 类 / 接口 / 命令 | 实现内容 |
| -------------- | ----------------------- | -------- |
| `path/to/file` | `xxx()`                 | ...      |

根据实际方案章节继续补充。

------

## 5. 新增文件

| 文件               | 作用 | 对应方案章节 | 说明 |
| ------------------ | ---- | ------------ | ---- |
| `path/to/new_file` | ...  | 第 X 节：... | ...  |

如果没有新增文件，写：

```
本次实现没有新增文件。
```

------

## 6. 修改文件

| 文件                    | 修改内容 | 对应方案章节 | 说明 |
| ----------------------- | -------- | ------------ | ---- |
| `path/to/existing_file` | ...      | 第 X 节：... | ...  |

------

## 7. 删除文件

| 文件                   | 删除原因 | 对应方案章节 | 说明 |
| ---------------------- | -------- | ------------ | ---- |
| `path/to/deleted_file` | ...      | 第 X 节：... | ...  |

如果没有删除文件，写：

```
本次实现没有删除文件。
```

------

## 8. 新增或修改的函数 / 类 / 接口 / 命令

### 8.1 新增函数

| 函数    | 所在文件       | 职责 | 对应方案章节 |
| ------- | -------------- | ---- | ------------ |
| `xxx()` | `path/to/file` | ...  | 第 X 节：... |

如果没有新增函数，写：

```
本次实现没有新增函数。
```

### 8.2 修改函数

| 函数    | 所在文件       | 修改内容 | 对应方案章节 |
| ------- | -------------- | -------- | ------------ |
| `yyy()` | `path/to/file` | ...      | 第 X 节：... |

如果没有修改函数，写：

```
本次实现没有修改已有函数。
```

### 8.3 新增或修改的类

| 类         | 所在文件       | 新增 / 修改 | 职责 | 对应方案章节 |
| ---------- | -------------- | ----------- | ---- | ------------ |
| `XxxClass` | `path/to/file` | 新增 / 修改 | ...  | 第 X 节：... |

如果没有新增或修改类，写：

```
本次实现没有新增或修改类。
```

### 8.4 新增或修改的接口

| 接口       | 方法                      | 所在文件       | 新增 / 修改 | 对应方案章节 |
| ---------- | ------------------------- | -------------- | ----------- | ------------ |
| `/api/xxx` | GET / POST / PUT / DELETE | `path/to/file` | 新增 / 修改 | 第 X 节：... |

如果没有新增或修改接口，写：

```
本次实现没有新增或修改接口。
```

### 8.5 新增或修改的命令 / 脚本

| 命令 / 脚本 | 所在文件       | 新增 / 修改 | 用途 | 对应方案章节 |
| ----------- | -------------- | ----------- | ---- | ------------ |
| `command`   | `path/to/file` | 新增 / 修改 | ...  | 第 X 节：... |

如果没有新增或修改命令 / 脚本，写：

```
本次实现没有新增或修改命令 / 脚本。
```

------

## 9. 数据结构和配置实现情况

### 9.1 请求结构

是否实现方案中的请求结构：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

### 9.2 响应结构

是否实现方案中的响应结构：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

### 9.3 数据库字段 / 数据模型

是否实现方案中的数据库字段或数据模型：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

### 9.4 状态枚举

是否实现方案中的状态枚举：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

### 9.5 配置项 / 环境变量

是否实现方案中的配置项或环境变量：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

### 9.6 依赖变更

是否实现方案中的依赖变更：

```
是 / 否 / 不涉及
```

实现位置：

- ...

说明：

```
...
```

------

## 10. 方案外的必要实现细节

记录实现过程中为了落地方案而补充的低层细节。

注意：

这里不记录新需求，只记录必要实现选择。

| 实现细节 | 所在文件       | 选择原因                       | 是否改变方案行为 |
| -------- | -------------- | ------------------------------ | ---------------- |
| ...      | `path/to/file` | 方案未指定，按项目现有风格实现 | 否               |

如果没有方案外实现细节，写：

```
本次实现没有添加方案外的必要实现细节。
```

------

## 11. 验证记录

### 11.1 已执行验证

| 验证项 | 命令 / 操作 | 结果          | 说明 |
| ------ | ----------- | ------------- | ---- |
| ...    | ...         | 通过 / 未通过 | ...  |

### 11.2 未执行验证

| 未验证项 | 原因 | 后续建议 |
| -------- | ---- | -------- |
| ...      | ...  | ...      |

如果没有执行验证，写：

```
本次未执行自动化验证。后续需要根据方案文件中的测试和验收方案进行验证。
```

------

## 12. 已知问题和后续处理

| 问题 | 影响范围 | 当前处理 | 后续建议 |
| ---- | -------- | -------- | -------- |
| ...  | ...      | ...      | ...      |

如果没有已知问题，写：

```
当前没有记录到已知问题。
```

------

## 13. 后续审查建议

建议审查以下内容：

1. 方案内容是否都已在第 3 节中对应到代码位置
2. 新增和修改文件是否符合方案范围
3. 函数、类、接口是否符合方案命名和职责
4. 数据结构和配置是否符合方案要求
5. 是否存在方案外功能
6. 验证记录是否足够支撑本次实现完成

------

## 14. 修改记录

### 第 1 次实现

实现内容：

修改文件：

实现时间：

### 第 2 次实现 / 修改

用户反馈：

修改内容：

修改文件：

修改时间：

### 第 3 次实现 / 修改

用户反馈：

修改内容：

修改文件：

修改时间：