# 修改功能方案：<功能名称>

## 0. 文档信息

功能名称：

方案类型：老项目 / 修改功能

方案状态：draft

创建时间：

最后更新时间：

方案文件路径：

关联需求：

关联代码实现任务：

相关旧功能：

------

## 1. 修改目标

### 1.1 用户想要改变什么

- ...

### 1.2 修改后的目标效果

- ...

### 1.3 本次不处理的内容

- ...

------

## 2. 旧功能现状分析

### 2.1 旧功能入口

说明旧功能从哪里进入。

例如：

- API 路由：
- 页面入口：
- 命令行入口：
- 定时任务入口：
- worker 入口：
- 第三方回调入口：

### 2.2 旧功能调用链

```
用户操作 / 外部输入
↓
旧入口函数 A
↓
旧业务函数 B
↓
旧数据处理函数 C
↓
数据库 / 外部服务 / 文件系统
↓
旧返回结果
```

### 2.3 旧功能当前行为

说明旧功能现在实际如何工作：

1. ...
2. ...
3. ...

### 2.4 旧接口请求结构

```
...
```

### 2.5 旧接口响应结构

```
...
```

### 2.6 旧数据结构

包括旧数据库字段、旧对象结构、旧缓存结构、旧队列消息结构等。

```
...
```

### 2.7 旧状态流转

```
状态 A
↓
状态 B
↓
状态 C
```

如果不涉及状态流转，写：

```
本功能不涉及状态流转。
```

### 2.8 旧异常和边界行为

| 场景 | 旧处理方式 | 旧返回结果 |
| ---- | ---------- | ---------- |
| ...  | ...        | ...        |

------

## 3. 新功能目标行为

### 3.1 新功能行为描述

说明修改后功能应该如何工作：

1. ...
2. ...
3. ...

### 3.2 新接口请求结构

```
...
```

### 3.3 新接口响应结构

```
...
```

### 3.4 新数据结构

```
...
```

### 3.5 新状态流转

```
状态 A
↓
状态 B
↓
状态 C
```

如果不涉及状态流转，写：

```
本功能不涉及状态流转变化。
```

------

## 4. 新旧行为对比

| 对比项     | 旧行为 | 新行为 | 是否兼容旧逻辑 | 说明 |
| ---------- | ------ | ------ | -------------- | ---- |
| 功能入口   | ...    | ...    | 是 / 否        | ...  |
| 请求参数   | ...    | ...    | 是 / 否        | ...  |
| 响应结构   | ...    | ...    | 是 / 否        | ...  |
| 数据库字段 | ...    | ...    | 是 / 否        | ...  |
| 状态流转   | ...    | ...    | 是 / 否        | ...  |
| 异常处理   | ...    | ...    | 是 / 否        | ...  |
| 权限逻辑   | ...    | ...    | 是 / 否        | ...  |
| 前端展示   | ...    | ...    | 是 / 否        | ...  |
| 测试用例   | ...    | ...    | 是 / 否        | ...  |

------

## 5. 兼容策略

### 5.1 旧接口兼容

是否保留旧接口：

是否支持旧请求参数：

是否保持旧响应字段：

是否需要新增响应字段：

说明：

```
...
```

### 5.2 旧数据兼容

是否兼容历史数据：

是否保留旧字段：

是否需要默认值：

是否需要数据转换：

说明：

```
...
```

### 5.3 旧调用方兼容

是否影响前端调用：

是否影响后端内部调用：

是否影响定时任务：

是否影响 worker：

是否影响第三方回调：

说明：

```
...
```

### 5.4 不兼容项说明

如果存在不兼容项，逐项说明：

1. 不兼容项：
   - 原因：
   - 影响范围：
   - 是否可接受：
   - 替代方案：

如果不存在不兼容项，写：

```
本次修改不引入不兼容变化。
```

------

## 6. 文件变更计划

### 6.1 新增文件

1. `path/to/new_file`
   - 作用：
   - 新增原因：

如果不需要新增文件，写：

```
本次修改不需要新增文件。
```

### 6.2 修改文件

1. `path/to/existing_file`
   - 修改位置：
   - 当前职责：
   - 修改原因：
   - 修改内容概述：
   - 是否影响旧逻辑：

### 6.3 删除文件

1. `path/to/deleted_file`
   - 删除原因：
   - 是否有替代文件：
   - 风险：

如果不需要删除文件，写：

```
本次修改不删除已有文件。
```

------

## 7. 依赖、扩展包和配置变化

### 7.1 依赖变化

是否新增第三方依赖：

是否新增框架扩展包：

是否升级已有依赖：

是否修改依赖版本：

是否新增系统组件或运行环境支持：

### 7.2 配置变化

是否修改配置文件：

是否新增环境变量：

是否修改路由配置：

是否修改权限配置：

是否修改中间件配置：

是否修改任务队列配置：

是否修改缓存配置：

是否修改第三方服务配置：

是否影响部署：

### 7.3 具体说明

如果有变化，逐项说明：

1. 变更项：
   - 文件位置：
   - 修改位置：
   - 修改原因：
   - 影响范围：
   - 是否有替代方案：

如果没有变化，写：

```
本次修改不需要新增依赖、扩展包或配置变化。
```

------

## 8. 最小修改原则

说明本方案如何做到最小改动：

- 是否只修改实现新行为必须修改的地方：
- 是否避免重构无关代码：
- 是否保持能兼容的旧接口格式不变：
- 是否保持能兼容的旧数据结构不变：
- 是否避免调整无关目录结构：
- 是否避免引入新依赖：
- 是否保留仍需要的旧逻辑：
- 是否避免一次性修改过多模块：

如果必须修改旧逻辑，说明原因和影响范围：

```
...
```

------

## 9. 函数设计和调用链

### 9.1 新调用链

```
用户操作 / 外部输入
↓
入口函数 A
↓
业务函数 B
↓
数据处理函数 C
↓
数据库 / 外部服务 / 文件系统
↓
返回结果
```

### 9.2 新增函数

#### 函数 1：xxx(...)

所在文件：

职责：

输入：

输出：

调用关系：

- 被谁调用：
- 调用谁：

### 9.3 修改函数

#### 函数 1：yyy(...)

所在文件：

当前职责：

修改原因：

修改后的职责：

输入变化：

输出变化：

调用关系变化：

### 9.4 保持不变的关键函数

1. 函数名：
   - 所在文件：
   - 保持不变原因：
   - 与本次修改的关系：

------

## 10. 数据结构变化

### 10.1 请求结构变化

旧结构：

```
...
```

新结构：

```
...
```

变化说明：

- ...

### 10.2 响应结构变化

旧结构：

```
...
```

新结构：

```
...
```

变化说明：

- ...

### 10.3 service 层输入结构变化

旧结构：

```
...
```

新结构：

```
...
```

变化说明：

- ...

### 10.4 service 层输出结构变化

旧结构：

```
...
```

新结构：

```
...
```

变化说明：

- ...

### 10.5 数据库字段变化

新增字段：

```
...
```

修改字段：

```
...
```

删除字段：

```
...
```

如果不涉及数据库字段变化，写：

```
本次修改不涉及数据库字段变化。
```

### 10.6 枚举值变化

旧枚举：

```
...
```

新枚举：

```
...
```

变化说明：

- ...

### 10.7 队列消息结构变化

旧结构：

```
...
```

新结构：

```
...
```

如果不涉及队列，写：

```
本次修改不涉及队列消息结构变化。
```

### 10.8 缓存结构变化

旧结构：

```
...
```

新结构：

```
...
```

如果不涉及缓存，写：

```
本次修改不涉及缓存结构变化。
```

### 10.9 配置结构变化

旧结构：

```
...
```

新结构：

```
...
```

如果不涉及配置结构，写：

```
本次修改不涉及配置结构变化。
```

### 10.10 字段说明要求

字段说明必须包含：

- 字段名：
- 底层类型：
- 含义：
- 示例值：
- 是否必填：
- 可能取值：
- 是否兼容旧数据：

数组说明格式：

```
字段名：xxx
类型：list[dict]

数组元素结构：

{
  "field_a": "str",
  "field_b": "int"
}
```

对象说明格式：

```
结构名：XxxObject

{
  "field_a": "str",
  "field_b": "int"
}
```

------

## 11. 数据迁移策略

### 11.1 是否需要数据迁移

需要 / 不需要

原因：

### 11.2 迁移内容

如果需要迁移，说明：

1. 迁移对象：
   - 原数据：
   - 新数据：
   - 转换规则：
   - 默认值：
   - 失败处理：

### 11.3 历史数据兼容

历史数据是否仍可读取：

历史数据是否需要补字段：

历史数据是否需要转换状态：

说明：

```
...
```

### 11.4 迁移回滚思路

如果迁移失败，如何回滚：

1. ...
2. ...

如果不需要迁移，写：

```
本次修改不涉及数据迁移。
```

------

## 12. 简单用例和完整数据流

### 12.1 示例场景

用户要：

### 12.2 旧流程数据流

#### 原始输入

```
...
```

#### 旧入口层输入

```
...
```

#### 旧业务层输出

```
...
```

#### 旧最终输出

```
...
```

### 12.3 新流程数据流

#### 原始输入

```
...
```

#### 第 1 层：入口函数 xxx()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

#### 第 2 层：业务函数 yyy()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

#### 第 3 层：数据处理 / 数据访问函数 zzz()

输入：

```
...
```

处理逻辑：

1. ...
2. ...

输出：

```
...
```

#### 新最终输出

```
...
```

### 12.4 新旧数据流差异

| 层级     | 旧数据 | 新数据 | 变化原因 |
| -------- | ------ | ------ | -------- |
| 原始输入 | ...    | ...    | ...      |
| 入口层   | ...    | ...    | ...      |
| 业务层   | ...    | ...    | ...      |
| 数据层   | ...    | ...    | ...      |
| 最终输出 | ...    | ...    | ...      |

------

## 13. 异常和边界情况

| 场景           | 旧处理方式 | 新处理方式 | 新返回结果 | 是否兼容旧行为 |
| -------------- | ---------- | ---------- | ---------- | -------------- |
| 输入为空       | ...        | ...        | ...        | 是 / 否        |
| 参数类型错误   | ...        | ...        | ...        | 是 / 否        |
| 数据不存在     | ...        | ...        | ...        | 是 / 否        |
| 数据重复       | ...        | ...        | ...        | 是 / 否        |
| 状态不允许     | ...        | ...        | ...        | 是 / 否        |
| 权限不足       | ...        | ...        | ...        | 是 / 否        |
| 并发重复提交   | ...        | ...        | ...        | 是 / 否        |
| 外部接口失败   | ...        | ...        | ...        | 是 / 否        |
| 数据库写入失败 | ...        | ...        | ...        | 是 / 否        |
| 历史数据不兼容 | ...        | ...        | ...        | 是 / 否        |

------

## 14. 旧行为依赖点扫描

### 14.1 旧接口调用方

| 调用方 | 位置 | 依赖的旧行为 | 是否需要修改 |
| ------ | ---- | ------------ | ------------ |
| ...    | ...  | ...          | 是 / 否      |

### 14.2 旧响应结构依赖点

| 位置 | 依赖字段 | 修改后是否保留 | 处理方式 |
| ---- | -------- | -------------- | -------- |
| ...  | ...      | 是 / 否        | ...      |

### 14.3 旧数据库字段依赖点

| 位置 | 依赖字段 | 修改后是否保留 | 处理方式 |
| ---- | -------- | -------------- | -------- |
| ...  | ...      | 是 / 否        | ...      |

### 14.4 旧状态枚举依赖点

| 位置 | 依赖状态 | 修改后是否保留 | 处理方式 |
| ---- | -------- | -------------- | -------- |
| ...  | ...      | 是 / 否        | ...      |

### 14.5 旧测试依赖点

| 测试文件 | 依赖的旧行为 | 是否需要更新 |
| -------- | ------------ | ------------ |
| ...      | ...          | 是 / 否      |

### 14.6 其他依赖点

说明是否存在以下依赖：

- 前端页面依赖
- 定时任务依赖
- worker 依赖
- 第三方回调依赖
- 缓存 key 依赖
- 配置项依赖
- 权限逻辑依赖
- 文档说明依赖

------

## 15. 测试和验收方案

### 15.1 旧功能回归测试

1. ...
   - 输入：
   - 旧预期：
   - 修改后预期：

### 15.2 新功能行为测试

1. ...
   - 输入：
   - 期望输出：

### 15.3 兼容性测试

1. ...
   - 输入：
   - 期望输出：

### 15.4 数据迁移测试

1. ...
   - 迁移前数据：
   - 迁移后数据：
   - 期望结果：

如果不涉及数据迁移，写：

```
本次修改不需要数据迁移测试。
```

### 15.5 异常流程测试

1. ...
   - 输入：
   - 期望输出：

### 15.6 边界情况测试

1. ...
   - 输入：
   - 期望输出：

### 15.7 手动验证步骤

1. ...
2. ...
3. ...

### 15.8 验收标准

做到以下结果，才算方案完整：

1. 旧功能依赖点已经识别清楚。
2. 新旧行为差异已经说明清楚。
3. 兼容策略已经说明清楚。
4. 数据结构变化已经说明清楚。
5. 数据迁移策略已经说明清楚或明确不需要迁移。
6. 新行为测试和旧功能回归测试都已覆盖。
7. 风险和回滚思路已经说明清楚。

------

## 16. 风险和回滚思路

### 16.1 主要风险

1. ...
2. ...

### 16.2 风险影响范围

| 风险 | 影响范围 | 严重程度     | 预防方式 |
| ---- | -------- | ------------ | -------- |
| ...  | ...      | 高 / 中 / 低 | ...      |

### 16.3 回滚思路

1. ...
2. ...

### 16.4 不可逆变化说明

如果存在不可逆变化，说明：

1. 不可逆项：
   - 原因：
   - 影响：
   - 是否可以接受：

如果不存在不可逆变化，写：

```
本次修改不涉及不可逆变化。
```

------

## 17. 用户审查记录

### 第 1 次审查

用户反馈：

修改内容：

修改时间：

### 第 2 次审查

用户反馈：

修改内容：

修改时间：

### 第 3 次审查

用户反馈：

修改内容：

修改时间：